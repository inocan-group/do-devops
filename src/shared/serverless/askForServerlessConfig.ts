/**
 * For people using the `typescript-microservice` template,
 * this function will interactively ask a user questions
 * so that the serverless configuration can be built.
 */
export async function askForServerlessConfig() {
  //
}
