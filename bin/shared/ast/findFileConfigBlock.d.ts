/**
 * Given a handler file, this will return the object key/value
 * pairs of the file's `config` export.
 */
export declare function findHandlerConfig(filename: string): import("./namedExports").IPropertyInfo[];
