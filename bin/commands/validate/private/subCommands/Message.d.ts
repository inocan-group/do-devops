import { ValidationAction } from "../../public";
import { IDictionary } from "common-types";
export declare function handler(action: ValidationAction, currentBranch: string, options?: IDictionary): Promise<1 | 0>;
