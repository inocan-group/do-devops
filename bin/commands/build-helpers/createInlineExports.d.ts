/**
 * Writes the serverless configuration file which contains
 * all the _inline_ function definitions found under `src/handlers`.
 */
export declare function createInlineExports(files: string[]): Promise<void>;
