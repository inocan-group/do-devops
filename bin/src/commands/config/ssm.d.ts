export interface IDoSsmConfig {
    /**
     * If the `findProfileIn` hint doesn't detect a profile then the fallback
     * is the `defaultProfile`
     */
    defaultProfile?: string;
}
export declare function ssm(): {};
